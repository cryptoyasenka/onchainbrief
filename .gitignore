# secrets — never commit
.env
*.env
.env.*
# .env.example is a tracked deliverable — the only un-ignored env file
!.env.example
*.key
*.pem
keypair*.json
secrets/

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
dist/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# local artifacts
*.log
.gstack/
.state/
.planning/ACE-MEASUREMENTS.md

# generated runtime artifacts (real ones come from a funded run + deploy)
# /briefs/
# /site/
/demo-out/

# public-repo scrub build dir
/.public-build/

# typescript SAP shim — keep src in repo, never node_modules
sap/node_modules/
sap/dist/
*.tsbuildinfo
